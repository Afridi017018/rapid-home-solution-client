import React from 'react';
import CartItem from '../CartItem/CartItem';

const CartItems = () => {
    return (
        <div>
            <CartItem />
            <CartItem />
            <CartItem />
            <CartItem />
            <CartItem />
        </div>
    );
};

export default CartItems;